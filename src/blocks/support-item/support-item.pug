mixin support-item(picture, content, additionalClass)
  .support-item(class=additionalClass)
    img.support-item__picture(src="img/"+picture, alt="Picture")
    a.support-item__title(href="#") #{content}
    +price('15 000')