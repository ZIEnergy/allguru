mixin item(picture, title, price, content, additionalClass)
  .item(class=additionalClass)
    img.item__picture(src="img/"+picture, alt="Photo")
    .item__content
      a.item__title(href="#") #{title}
      +price(price)
      p.item__dscr #{content}
      +meta